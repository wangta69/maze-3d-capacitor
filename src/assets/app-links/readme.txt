2type check
1. app start -> init.service -> platform.ready -> getGameLinks -> read links.json  from local -> transferGameLinks (read links.json from server) -> compareVersion

so just change version if icon image is updated
