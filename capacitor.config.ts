import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'game.pondol.matchblock.app',
  appName: 'Match Block',
  webDir: 'dist/game-match-3',
  bundledWebRuntime: false
};

export default config;
